data:
  batch_size: 256
  shuffle: True
  num_workers: 2
  mode: "subtract"
  p_quant: 112
  window_len: 112
  random_seed: 42
training:
  num_epochs: 20
  lr: 0.001
  min_lr: 0.0005
  weight_decay:  0.00005
model:
  model_dir: "saved_models"
  name: "VIT_b_32"
  dropout: 0.5
